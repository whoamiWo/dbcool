# MVP 范围定义

> 创建日期: 2026-09-09
> 关联: IDEA_BRIEF.md, USER_STORIES.md
> 目标: 一个完整可演示的"低代码平台 v1.0"

---

## 一、MVP 边界原则

**MVP 的目标不是"能用",而是"能让非工程师独立做出第一个应用"。**

为了避免范围蔓延,所有功能按以下规则取舍:

| 必须做 | 可以做 | 明确不做 |
|---|---|---|
| 没有它业务用户无法自服务 | 不影响"独立做出第一个应用" | 与"自服务搭建"无关 |
| 演示当天必须可演示 | 演示完后再补 | 与"差异化"无关 |

## 二、必须有的 5 大能力

### 能力 1:数据模型引擎(Collection Engine)
- ✅ 增 / 删 / 改 表(运行时)
- ✅ 增 / 删 / 改 字段(运行时)
- ✅ 字段类型(必含):
  - text / textarea / number / boolean / date / datetime
  - select (单选) / multiSelect (多选)
  - attachment (附件)
  - belongsTo / hasMany (关联)
  - formula (公式)
- ✅ 主键、必填、唯一、默认值
- ✅ 关联关系(一对一、一对多)

### 能力 2:表单设计器(Form Builder)
- ✅ 拖拽添加字段
- ✅ 字段属性面板(标签、占位符、帮助文案、必填)
- ✅ 显隐规则(基于其他字段值)
- ✅ 数据校验(必填、长度、范围、正则)
- ✅ 提交按钮 + 提交后动作(跳转、弹提示)

### 能力 3:视图设计器(View Builder)
- ✅ 表格视图(列选择、筛选、排序、分页)
- ✅ 看板视图(按某字段分组)
- ✅ 详情视图(单条记录展示)
- ✅ 表单视图(对应能力 2)
- ✅ 视图共享给其他用户

### 能力 4:权限系统(ACL)
- ✅ 用户管理(admin 角色)
- ✅ 角色管理(自定义角色)
- ✅ 字段级权限(可见 / 可编辑 / 隐藏)
- ✅ 记录级权限(行过滤条件)
- ✅ 操作权限(增 / 删 / 改 / 查)

### 能力 5:工作流引擎(Workflow)
- ✅ 触发器:
  - 数据变化触发(创建 / 更新 / 删除)
  - 定时触发(每天 8 点)
  - 手动触发
- ✅ 节点类型:
  - 审批节点(单人 / 多人会签)
  - 通知节点(站内信 / 邮件)
  - 条件分支(if/else)
  - 数据更新节点
  - HTTP 调用节点
- ✅ 流程状态跟踪(谁审批了、当前在哪)

## 三、明确不在 MVP 内(给开发者和产品团队看的清单)

| 功能 | 不做原因 | 计划版本 |
|---|---|---|
| 移动端 App | 响应式 Web 已覆盖 | V1.1 |
| 多语言 | 中文字段名支持,界面先中文 | V1.2 |
| 公开注册 | B 端不开 | 永不 |
| 第三方登录(OAuth) | admin 开账号足够 | V2.0 |
| 实时多人协作 | Yjs 复杂度高 | V2.0 |
| 数据透视表 | 用视图代替 | V2.0 |
| 自定义主题色 | 默认主题够用 | V1.1 |
| 审计日志 UI | 写日志,但 UI V1.2 | V1.2 |
| 移动端扫码 | 不做 | 永不 |
| 复杂公式引擎 | 用 formula 字段,不支持 if/while | V2.0 |
| 移动端推送 | 不做 App | 永不 |
| 工作流子流程 | 单层审批够 | V1.1 |
| 多租户 UI | 架构支持,UI 单租户 | V1.1 |
| API 限流配置 | 写死默认值 | V1.2 |
| 数据导出 CSV | 暂用浏览器原生 | V1.1 |
| 数据导入 CSV | 手动录入 | V2.0 |

## 四、用户角色(MVP)

| 角色 | 能力 |
|---|---|
| **平台 Admin** | 管用户、管角色、查审计日志 |
| **应用开发者**(业务人员) | 建表、建表单、建视图、建工作流、分享 |
| **普通用户** | 用应用开发者搭好的应用,提交/查看数据 |

## 五、成功指标(演示当天必须满足)

### 功能演示场景
- 一个从未用过的业务人员,1 小时内能:
  1. 创建一个"客户投诉"表(10 个字段,含关联)
  2. 设计"提交投诉"表单
  3. 设计"投诉列表"视图
  4. 设置"部门经理才能看到投诉"权限
  5. 设置"提交后通知部门经理"工作流

### 性能
- 200 并发用户,典型页面 p95 < 500ms
- 100 万条记录的单表查询 < 1s

### 稳定性
- 7×24 演示期间 0 崩溃
- 数据库 / Redis 重启后服务自动恢复

## 六、风险与对策

| 风险 | 概率 | 影响 | 对策 |
|---|---|---|---|
| Collection 引擎 ALTER TABLE 慢 | 高 | 高 | 用 JSONB fallback + 后台异步迁移 |
| 工作流引擎复杂度低估 | 中 | 高 | 先做"数据触发 + 审批 + 通知"3 节点,其他后补 |
| 拖拽设计器性能瓶颈 | 中 | 中 | 虚拟化 + 防抖 |
| 单兵开发 6 个月太紧 | 高 | 高 | 砍功能而非延期,优先保证核心 5 能力 |

## 七、演示数据准备

演示当天需要预置:
- 3 个示例应用(客户管理 / 设备台账 / 审批流程)
- 5 个示例用户(admin / 经理 / 员工 / 财务 / 客户)
- 100 条示例数据
