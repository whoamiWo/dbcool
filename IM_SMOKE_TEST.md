# IM 端到端联调指令(给 Cline 执行,由 CodeBuddy 审查)

> 代码已通过构建验证(后端 891 测试、前端 159 测试、`pnpm build` 成功),
> 但**尚未在浏览器真实跑过**。本次任务就是补上这一环。
>
> **核心要求:每一项都必须贴原始证据。**
> 不接受"已验证""已完成""功能正常"这类无证据结论 ——
> 前两次交付的问题都出在"写完就交、没实跑",本次必须杜绝。

---

## 给 Cline 的话(可直接复制执行)

```
请按项目根目录 IM_SMOKE_TEST.md 执行 IM 端到端联调。

硬性要求:
1. 起真实服务,用浏览器实际操作,不要只跑单元测试就下结论。
2. 10 个检查点每一项都要按文档末尾的模板提交「操作 / 结果 / 证据」三部分。
   证据必须是原始输出 —— 控制台原文、Network 的 URL 与状态码、或截图说明。
3. 不接受"已完成""已验证"这类没有证据的结论。
4. 失败的项必须贴完整错误原文(含堆栈),不要只写"报错了"。
5. 不得为了让检查通过而临时改代码绕过问题。发现问题就如实报告 ——
   报告问题不算失败,掩盖问题才算。
6. 若某项受环境限制无法执行(如没有两个账号),如实标注"环境受限未执行",
   不要伪造证据。
```

---

## 一、环境准备

### 1. 起依赖服务

```bash
docker compose up -d postgres redis
docker compose ps          # 确认两个容器状态为 healthy
```

端口:`postgres:5432`、`redis:6379`。

### 2. 起后端(Java)

```bash
cd backend-java && mvn -o spring-boot:run
```

端口 **8080**。等待出现 `Started NocoBaseApplication` 再继续。
若启动失败(Flyway 校验、端口占用等),**贴完整异常堆栈**,不要自行改配置绕过。

### 3. 起前端

```bash
cd frontend && pnpm dev
```

端口 **5173**。

代理已配置好(无需改动):
- `/api` → `localhost:8080`(Java)
- `/api/ai` → `localhost:8000`(Python)
- `/ws` → `localhost:8080`,且 `ws: true`

### 4. 准备两个账号

检查点 5(实时性)需要两个不同账号。可在管理后台创建第二个用户,
或用**普通窗口 + 无痕窗口**分别登录。确实无法准备两个账号时,
标注"环境受限未执行",不要伪造。

---

## 二、10 个检查点

### 检查点 1:接口路径正确(防 404 复发)

- **操作**:浏览器登录 → 打开 `http://localhost:5173/im` → 打开 DevTools Network 面板
- **预期**:`GET /api/im/channels` 返回 **200**
- **必查**:URL 必须是**单层** `/api`。若出现 `/api/api/im/channels` 即为回归(历史阻断缺陷)
- **证据**:贴出该请求的 **完整 URL 与状态码**

### 检查点 2:页面不白屏(防崩溃复发)

- **操作**:停留在 `/im` 页面,查看 Console
- **预期**:正常渲染三栏布局,**无**以下错误:
  ```
  TypeError: There is no underlying STOMP connection
  ```
- **证据**:贴 Console 面板内容(无错误也请写明"Console 无报错")

### 检查点 3:新建频道

- **操作**:点击频道列表右上角的「新建」按钮,创建 `PUBLIC` 类型频道
- **预期**:新频道出现在左侧列表
- **证据**:贴创建后的列表截图说明,以及 `POST /api/im/channels` 的状态码

### 检查点 4:发送消息

- **操作**:选中频道,在输入区发一条消息(Enter 发送)
- **预期**:消息出现在右下方消息流
- **证据**:贴 `POST /api/im/messages` 的状态码与消息流截图说明

### 检查点 5:实时性(最关键)

- **操作**:两个浏览器(或普通 + 无痕)分别登录**不同账号**,进入**同一频道**。
  A 发送一条消息,**B 页面不做任何刷新**
- **预期**:B 页面**立即**出现该消息
- **为何关键**:这是重写后的 `stompClient` 核心价值 ——
  `pendingSubs` 待订阅队列 + `onConnect` 补建。单窗口测试无法验证
- **证据**:贴两个窗口的截图说明(需能看出是同一消息、且 B 未刷新);
  如有条件,可补充 DevTools → WS 面板中收到的 frame

### 检查点 6:未读角标

- **操作**:B 账号**不打开** A 发消息的频道,停留在列表
- **预期**:该频道右侧出现红色未读数字(如 `1`)
- **证据**:贴列表截图说明,以及 `GET /api/im/messages/unread?channelId=...` 的返回值

### 检查点 7:表情回应

- **操作**:对一条消息点表情 → 再点一次取消 → 刷新页面
- **预期**:点一次数字 +1,再点取消,**刷新后状态保留**
- **证据**:贴 `POST`/`DELETE /api/im/messages/{id}/reactions` 的状态码,
  以及刷新前后的状态说明

### 检查点 8:cursor 翻页

- **操作**:在同一频道连续发送 **50 条以上**消息,然后向上滚动/点击加载更早
- **预期**:能加载出更早的历史消息,`next_cursor` 生效
- **证据**:贴 `GET /api/im/messages?channelId=...&cursor=...&limit=50`
  中带 `cursor` 参数的请求 URL,以及加载到历史消息的数量说明

### 检查点 9:软删除保留线程

- **操作**:删除自己刚发的一条消息
- **预期**:该消息**不消失**,显示为「该消息已删除」(灰色)
- **为何关键**:这是既定约定 —— 保留线程完整性。若被改成隐藏即为回归
- **证据**:贴删除后消息流的截图说明(需能看出显示的是「该消息已删除」)

### 检查点 10:登出断开 WS

- **操作**:进入 `/im` 建立连接后,点击退出登录
- **预期**:DevTools → Network → **WS** 面板中连接变为 **Closed**,
  且**不再自动重连**
- **证据**:贴 WS 面板在登出前后的状态说明

---

## 三、提交模板(每项必填)

```
### 检查点 N:<名称>
- 操作:<具体执行了什么>
- 结果:✅ 通过 / ❌ 失败 / ⚠️ 环境受限未执行
- 证据:
  <控制台输出原文 / 请求 URL 与状态码 / 截图说明>
```

最后请附一段汇总:

```
## 汇总
- 通过:X / 10
- 失败:<列出编号>
- 环境受限:<列出编号及原因>
- 后端启动是否成功:是/否
- 前端 dev 是否成功:是/否
```

---

## 四、已知预期现象(不要误判为缺陷)

| 现象 | 说明 | 该怎么做 |
|---|---|---|
| **告警页 `/alerts` 无实时数据** | Java 侧 `collector.emit()` 在生产代码**零调用**,告警数据源尚未接入。这是**遗留问题,不是本次缺陷** | 如实记录为"无数据(预期)",**不要**为了看到效果而新增 `emit()` 调用或造假数据 |
| **Redis 未启动时跨实例桥接失效** | `RedisStompBridge` 依赖 Redis | 单实例下订阅仍应正常。若 Redis 未起,标注即可 |
| **第一个账号没有频道** | 新建用户默认无频道 | 用检查点 3 的「新建」创建,或用管理员邀请 |
| **`/ws/alerts` 相关** | 旧的原生端点,无鉴权,本期不动 | 忽略即可 |

---

## 五、审查判据(供你自检)

我(CodeBuddy)会按以下标准审查你提交的内容:

1. **10 项是否齐全**,每项是否含「操作 / 结果 / 证据」三部分
2. **证据是否为原始输出** —— 只贴代码片段或只写"正常"的,视为未执行
3. **失败项是否有完整错误原文**,足以定位
4. **是否存在"看起来通过但实际没跑"的迹象** ——
   例如只贴代码不贴运行输出、请求 URL 与实际操作不符、截图描述含糊
5. **是否如实报告了问题** —— 报告问题不扣分,掩盖问题需要返工
