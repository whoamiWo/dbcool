# 产品差距分析报告

> 创建日期: 2026-09-17
> 更新日期: 2026-09-17（修正许可合规性、时间估算、产品命名）
> 关联: PRODUCT_ROADMAP.md, USER_STORIES.md, MVP_SCOPE.md
> 目标: 在现有 NocoBase 级低代码平台（P0 33/33 已达标）基础上，对标 NocoBase/NocoDB/Slack/RocketChat/Trello/Airtable，输出差距矩阵与优先级排序。
> 用户选择: 优先级=文档/Wiki+知识库；嵌入=钉钉优先；目标=200人中型团队
> 产品命名: DBCool（避开已有 WorkPro 商标冲突）

---

## 一、现有能力基线

### 1.1 技术栈

| 栈 | 技术 | 状态 |
|---|---|---|
| **Java (Spring Boot 3)** | 核心引擎、数据模型、权限、工作流、通知、IM | ✅ 成熟 |
| **Python (FastAPI)** | AI echo/chat、异步任务、告警 webhook | ⚠️ 基础 |
| **JS/TS (React 18)** | 可视化设计器、运行时渲染 | ✅ 成熟 |
| **数据层** | PostgreSQL(主) + Redis(缓存/实时) + MinIO(附件) + SQLite(告警) | ✅ 成熟 |

### 1.2 核心功能模块

| 模块 | 关键能力 | 状态 |
|---|---|---|
| **数据模型引擎** | Collection 运行时增删改、20+字段类型、关联(belongsTo/hasMany)、公式 | ✅ |
| **表单设计器** | 拖拽、字段属性面板、显隐规则、校验(必填/长度/范围/正则)、提交动作 | ✅ |
| **视图设计器** | 表格/看板/详情视图、筛选/排序/分页、视图共享 | ✅ |
| **权限系统 ACL** | 字段级(可见/可编辑/隐藏)、记录级(行过滤)、操作级、角色继承 | ✅ |
| **工作流引擎** | 5节点(审批/通知/条件/数据更新/HTTP)、3触发器(数据变化/定时/手动)、死循环防护 | ✅ |
| **IM 即时消息** | 频道(PUBLIC/PRIVATE/DIRECT)、消息游标分页、线程回复、软删除、表情、Redis在线状态 | ✅ 部分 |
| **通知渠道** | Email(Webhook真实SMTP)、Webhook、钉钉机器人、企业微信机器人 | ✅ 部分 |
| **多租户** | SCHEMA 隔离、Hibernate multiTenancy、租户管理 CRUD | ✅ |
| **插件系统** | PluginRegistry + 生命周期钩子 + 热重载 | ✅ |
| **审计日志** | 操作记录 + IP/User-Agent | ✅ |
| **API Key** | SHA-256 hash + 前缀 + 有效期 | ✅ |
| **健康检查** | liveness/readiness/连接池监控 | ✅ |
| **AI 辅助** | Python echo/whoami/chat(基础) | ⚠️ 基础 |

### 1.3 P0 完成度

- **33/33 = 100%** — MVP 达标线 ✅
- P1 done=5, partial=2, not-started=5
- P2 done=1, not-started=2

---

## 二、逐维度差距矩阵

### 2.1 对比维度定义

| 维度 | 说明 |
|---|---|
| **数据模型** | 数据表/字段/关联/公式能力 |
| **表单设计** | 拖拽表单、字段联动、校验 |
| **视图/报表** | 表格/看板/详情/数据透视/图表 |
| **权限系统** | 字段/记录/操作/角色/组织权限 |
| **工作流** | 可视化编排、节点类型、触发器 |
| **文档/Wiki** | 富文本/Markdown、版本、权限、嵌套、搜索 |
| **知识库** | 文档分类、标签、搜索、AI问答 |
| **IM 协作** | 频道/消息/线程/在线状态 |
| **通知** | 多渠道、模板、推送 |
| **集成市场** | 第三方应用市场、API 集成中心 |
| **项目管理** | 甘特图/看板/任务/里程碑 |
| **AI 能力** | LLM 辅助、AI 生成、AI 问答 |
| **移动端** | 响应式 Web / 原生 App |
| **实时协作** | Yjs/CRDT 多人协同编辑 |
| **部署/扩展** | Docker/K8s、插件、热重载 |
| **平台嵌入** | 钉钉/微信/飞书集成 |

### 2.2 差距矩阵（✅=已达标 ⚠️=部分 ✅=缺失）

| 维度 | 现有 | NocoBase | NocoDB | Slack | RocketChat | Trello | Airtable | Notion | 差距 |
|---|---|---|---|---|---|---|---|---|---|
| **数据模型** | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ | ✅ | ✅ | 已达标 |
| **表单设计** | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ | 已达标 |
| **视图/报表** | ⚠️ | ✅ | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | **部分**(缺数据透视/图表) |
| **权限系统** | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ | 已达标 |
| **工作流** | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ | 已达标 |
| **文档/Wiki** | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | **缺失** |
| **知识库** | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | **缺失** |
| **IM 协作** | ⚠️ | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ | ❌ | **部分**(缺频道管理/搜索) |
| **通知** | ⚠️ | ✅ | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ | **部分**(缺微信个人版/飞书) |
| **集成市场** | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ | **缺失** |
| **项目管理** | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ | ❌ | **缺失** |
| **AI 能力** | ⚠️ | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ | **部分**(缺完整LLM) |
| **移动端** | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ | **缺失** |
| **实时协作** | ❌ | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ | ✅ | **缺失** |
| **部署/扩展** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 已达标 |
| **平台嵌入** | ⚠️ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | **部分**(有钉钉webhook通知,缺应用中心/审批) |

### 2.3 差距优先级排序

| 优先级 | 能力 | 差距等级 | 对标产品 | 理由 |
|---|---|---|---|---|
| **P0** | **文档/Wiki** | 缺失 | Notion | 企业最常用协作入口，用户指定最高优先级 |
| **P0** | **知识库** | 缺失 | Notion | 文档的组织化与搜索 |
| **P1** | **钉钉嵌入(应用中心+审批)** | 部分 | 钉钉开放平台 | 用户指定钉钉优先 |
| **P1** | **IM 增强(频道管理/搜索)** | 部分 | Slack/RocketChat | 已有基础，需补全 |
| **P2** | **集成市场** | 缺失 | Airtable/Slack | 连接第三方应用 |
| **P2** | **AI 能力增强** | 部分 | Notion/Airtable | 完整 LLM 集成 |
| **P3** | **BI/数据透视** | 缺失 | Airtable | 数据分析 |
| **P3** | **项目管理(甘特/任务)** | 缺失 | Trello | 任务管理 |
| **P4** | **移动端** | 缺失 | Slack/Notion | 响应式Web可先覆盖 |
| **P4** | **实时协作(Yjs)** | 缺失 | Notion/Slack | 复杂度高，V2+ |
| **P4** | **微信(个人)集成** | 缺失 | 微信 | 企业微信已有，个人微信后续 |

### 2.4 NocoBase 许可合规性说明

> ⚠️ **重要**: NocoBase 采用 Apache-2.0 + 补充条款，明确禁止向公众提供 SaaS/PaaS 形式的低代码/AI 平台。

本项目是**自部署、内部使用**，完全合规：
- ✅ 自部署，不对外提供 SaaS/PaaS 服务
- ✅ 内部团队使用，不向公众开放
- ✅ 可移除 NocoBase 品牌信息（需商业许可）
- ✅ 可二次开发，无需遵守 NocoBase 补充条款

**建议**: 本项目代码与 NocoBase 保持独立，避免直接 fork，使用 Apache-2.0 开源协议。

---

## 三、文档/Wiki + 知识库能力方案

### 3.1 设计思路

**核心原则**: 复用现有 Collection 引擎（数据模型 + 权限 + 工作流），扩展文档/Wiki 能力，而非从零构建。

**为什么复用 Collection 引擎**:
- 文档本质是"有版本、有层级、有权限的结构化数据"
- Collection 引擎已支持: 运行时增删改、字段类型、关联、权限(字段/记录/操作级)、工作流
- 复用后可避免重复建设权限、ACL、工作流等基础设施

### 3.2 架构设计

```
┌─────────────────────────────────────────────────────┐
│                    前端 (React)                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐             │
│  │ Wiki编辑器 │ │ 知识库管理 │ │ 搜索中心  │             │
│  │ (Markdown) │ │ (知识树)  │ │ (全文检索) │             │
│  └──────────┘ └──────────┘ └──────────┘             │
└─────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────┐
│                  Java 后端                            │
│  ┌───────────────────────────────────────────┐      │
│  │  Collection 引擎 (复用)                    │      │
│  │  - 数据模型: 文档=Collection, 页面=Record   │      │
│  │  - 权限: 字段/记录/操作级 ACL               │      │
│  │  - 工作流: 文档审批/发布流程               │      │
│  └───────────────────────────────────────────┘      │
│  ┌───────────────────────────────────────────┐      │
│  │  新增模块: wiki/                           │      │
│  │  - WikiService: 文档CRUD、版本管理          │      │
│  │  - KnowledgeBaseService: 知识库管理          │      │
│  │  - SearchService: 全文检索                  │      │
│  └───────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────┐
│                  数据层                              │
│  - wiki_page 表 (文档页面)                          │
│  - wiki_version 表 (版本历史)                       │
│  - knowledge_base 表 (知识库)                       │
│  - wiki_category 表 (分类/标签)                     │
│  - 全文检索索引 (PostgreSQL FTS / Elasticsearch)     │
└─────────────────────────────────────────────────────┘
```

### 3.3 数据模型设计

#### 3.3.1 文档页面 (`wiki_page`)

```sql
CREATE TABLE wiki_page (
    id          UUID PRIMARY KEY,
    tenant_id   VARCHAR(64) NOT NULL,        -- 租户隔离
    knowledge_base_id UUID NOT NULL,          -- 所属知识库
    parent_id   UUID,                         -- 父页面(知识树层级)
    title       VARCHAR(256) NOT NULL,
    slug        VARCHAR(256) UNIQUE NOT NULL, -- URL 友好标识
    content     TEXT NOT NULL,                -- Markdown 内容
    content_html TEXT,                       -- 渲染后的 HTML(缓存)
    status      VARCHAR(20) NOT NULL DEFAULT 'DRAFT', -- DRAFT/PUBLISHED/ARCHIVED
    version     INT NOT NULL DEFAULT 1,       -- 当前版本号
    created_by  UUID NOT NULL,
    updated_by  UUID NOT NULL,
    created_at  TIMESTAMP NOT NULL,
    updated_at  TIMESTAMP NOT NULL,
    INDEX idx_tenant_kb (tenant_id, knowledge_base_id),
    INDEX idx_parent (parent_id),
    INDEX idx_slug (slug)
);
```

#### 3.3.2 版本历史 (`wiki_version`)

```sql
CREATE TABLE wiki_version (
    id          UUID PRIMARY KEY,
    wiki_page_id UUID NOT NULL REFERENCES wiki_page(id),
    version     INT NOT NULL,
    content     TEXT NOT NULL,                -- 该版本的 Markdown 内容
    summary     VARCHAR(512),                 -- 变更摘要
    created_by  UUID NOT NULL,
    created_at  TIMESTAMP NOT NULL,
    UNIQUE(wiki_page_id, version)
);
```

#### 3.3.3 知识库 (`knowledge_base`)

```sql
CREATE TABLE knowledge_base (
    id          UUID PRIMARY KEY,
    tenant_id   VARCHAR(64) NOT NULL,
    name        VARCHAR(128) NOT NULL,
    description VARCHAR(512),
    slug        VARCHAR(128) UNIQUE NOT NULL,
    icon        VARCHAR(64),
    sort_order  INT DEFAULT 0,
    created_by  UUID NOT NULL,
    created_at  TIMESTAMP NOT NULL,
    updated_at  TIMESTAMP NOT NULL
);
```

#### 3.3.4 分类/标签 (`wiki_category`)

```sql
CREATE TABLE wiki_category (
    id          UUID PRIMARY KEY,
    knowledge_base_id UUID NOT NULL,
    parent_id   UUID,                         -- 树形分类
    name        VARCHAR(128) NOT NULL,
    slug        VARCHAR(128) NOT NULL,
    sort_order  INT DEFAULT 0,
    UNIQUE(knowledge_base_id, slug)
);

CREATE TABLE wiki_page_tag (
    wiki_page_id UUID NOT NULL,
    tag          VARCHAR(64) NOT NULL,
    PRIMARY KEY (wiki_page_id, tag)
);
```

### 3.4 核心功能设计

#### 3.4.1 文档编辑器

| 功能 | 说明 | 对标 |
|---|---|---|
| Markdown 编辑 | 实时预览 + 源码编辑切换 | Notion |
| 富文本工具栏 | 加粗/列表/表格/代码块/引用/链接 | Notion |
| 嵌入 Collection 数据 | 通过 `[[]]` 语法嵌入数据库记录 | Notion |
| 附件上传 | MinIO 存储，拖拽上传 | Notion |
| 模板 | 预置模板 + 自定义模板 | Notion |

#### 3.4.2 版本管理

| 功能 | 说明 |
|---|---|
| 自动保存 | 每次编辑自动创建版本 |
| 版本对比 | Diff 视图展示版本差异 |
| 版本回滚 | 一键恢复到任意历史版本 |
| 变更摘要 | 每次编辑记录变更摘要 |

#### 3.4.3 知识树

| 功能 | 说明 |
|---|---|
| 层级结构 | 知识库 → 分类 → 页面(树形导航) |
| 拖拽排序 | 分类和页面可拖拽调整顺序 |
| 权限继承 | 父页面权限可继承给子页面 |
| 面包屑导航 | 当前路径清晰可见 |

#### 3.4.4 权限

| 功能 | 说明 |
|---|---|
| 页面级权限 | 可见/可编辑/可评论(复用 ACL) |
| 知识库级权限 | 整个知识库的访问控制 |
| 字段级权限 | 文档某些字段仅管理员可见 |
| 记录级权限 | 某些文档仅特定用户可见 |

#### 3.4.5 搜索

| 功能 | 说明 |
|---|---|
| 全文检索 | PostgreSQL FTS 或 Elasticsearch |
| 搜索范围 | 按知识库/分类/标签过滤 |
| 高亮显示 | 搜索关键词高亮 |
| 排序 | 按相关性/时间/浏览量排序 |

### 3.5 前端实现

#### 3.5.1 新增页面

| 页面 | 路径 | 功能 |
|---|---|---|
| Wiki 编辑器 | `/wiki/:slug` | Markdown 编辑 + 实时预览 |
| 知识库列表 | `/knowledge-bases` | 知识库管理入口 |
| 知识库管理 | `/knowledge-bases/:id` | 分类/页面/权限管理 |
| 搜索中心 | `/search` | 全文搜索 |

#### 3.5.2 复用组件

- `SchemaDesigner` → 文档 Schema 定义
- `FormDesigner` → 文档编辑表单
- `AclEditor` → 文档权限配置
- `WorkflowDesigner` → 文档审批/发布工作流

---

## 四、钉钉嵌入方案

### 4.1 钉钉开放平台能力

| 能力 | API/功能 | 说明 |
|---|---|---|
| **应用中心** | 微应用创建/发布/管理 | 将 NocoBase 封装为钉钉微应用 |
| **OA 审批** | 钉钉审批 API + 事件回调 | 将工作流审批同步到钉钉 OA |
| **消息推送** | 钉钉机器人 Webhook | 已有 DingTalkDispatcher |
| **SSO 登录** | 钉钉扫码登录/钉钉免密 | 用户通过钉钉单点登录 |
| **组织架构** | 钉钉通讯录 API | 同步用户/部门/角色 |
| **钉钉文档** | 钉钉文档开放 API | 嵌入钉钉文档能力 |
| **钉钉群** | 群机器人/群消息 | 工作流通知到钉钉群 |

### 4.2 集成架构

```
┌─────────────────────────────────────────────────────┐
│                    钉钉客户端                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐             │
│  │ 微应用    │ │ OA审批    │ │ 群机器人  │             │
│  │ (NocoBase)│ │ (工作流)  │ │ (通知)   │             │
│  └──────────┘ └──────────┘ └──────────┘             │
└─────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────┐
│                  NocoBase 后端                       │
│  ┌───────────────────────────────────────────┐      │
│  │  DingTalkAdapter (新增)                    │      │
│  │  - SSO 登录(钉钉免密)                      │      │
│  │  - 组织架构同步(用户/部门/角色)             │      │
│  │  - OA 审批回调(工作流→钉钉审批)            │      │
│  │  - 群消息推送(工作流通知→钉钉群)           │      │
│  └───────────────────────────────────────────┘      │
│  ┌───────────────────────────────────────────┐      │
│  │  已有: DingTalkDispatcher(webhook 通知)     │      │
│  └───────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────┘
```

### 4.3 实施路径

| 阶段 | 内容 | 优先级 |
|---|---|---|
| **Phase 1** | 钉钉 SSO 登录(免密) + 组织架构同步 | 高 |
| **Phase 2** | 微应用封装(钉钉应用中心上架) | 高 |
| **Phase 3** | OA 审批回调(工作流→钉钉审批) | 中 |
| **Phase 4** | 群消息推送(工作流通知→钉钉群) | 中 |
| **Phase 5** | 钉钉文档嵌入 | 低 |

### 4.4 SSO 登录设计

```
用户点击"钉钉登录"
    │
    ▼
NocoBase 重定向到钉钉授权页
    │
    ▼
用户扫码确认
    │
    ▼
钉钉回调携带 code
    │
    ▼
NocoBase 用 code 换取 access_token + 免密登录
    │
    ▼
调用钉钉通讯录 API 获取用户信息(部门/角色)
    │
    ▼
创建/更新本地用户(tenant_id + roles)
    │
    ▼
返回 JWT → 用户进入平台
```

---

## 五、分阶段路线图

### Phase 1: 文档/Wiki 核心（优先级最高）

**目标**: 实现文档编辑器 + 知识库管理 + 版本控制 + 搜索

| 周次 | 任务 | 验收 |
|---|---|---|
| W1~W2 | Wiki 数据模型(4 张表) + Flyway migration + WikiService CRUD | DDL 执行成功 + API 测试通过 |
| W3~W4 | Markdown 编辑器前端 + 实时预览 + 知识树导航 | 编辑器可用 + 树形结构可用 |
| W5~W6 | 权限集成(复用 ACL) + 页面级权限 + 全文检索(PostgreSQL FTS) | 权限控制生效 + 搜索可用 |
| W7~W8 | 工作流集成(文档审批/发布流程) + 版本管理 + 差异对比 | 工作流触发 + 版本回滚可用 |
| W9~W10 | 测试 + 文档 + 演示 + 用户验收 | 10 个 P0 故事全过 |

**新增用户故事**: US-601~610（文档/Wiki 10 个 P0 故事）

### Phase 2: 钉钉嵌入

**目标**: 钉钉 SSO + 微应用 + OA 审批回调

| 周次 | 任务 | 验收 |
|---|---|---|
| W11~W12 | DingTalkAdapter SSO 登录 + 组织架构同步 | 钉钉免密登录成功 + 通讯录同步 |
| W13~W14 | 微应用封装(钉钉应用中心) + OA 审批回调 | 微应用上架 + 审批同步 |
| W15~W16 | 群消息推送(工作流通知→钉钉群) + 测试 | 通知到达 + 5 个 P0 故事全过 |

**新增用户故事**: US-611~615（钉钉嵌入 5 个 P0 故事）

### Phase 3: 集成市场

**目标**: 第三方应用市场 + API 集成中心

| 周次 | 任务 | 验收 |
|---|---|---|
| W15 | 集成市场前端(应用列表/安装/配置) | 市场 UI 可用 |
| W16 | 集成市场后端(应用管理/权限) | API 可用 |
| W17 | 首批集成(钉钉/飞书/Slack/Webhook) | 3+ 集成可用 |
| W18 | 测试 + 演示 | 4 个 P0 故事全过 |

**新增用户故事**: US-614~617

### Phase 4: 增强能力

**目标**: AI 增强 + BI/数据透视 + IM 增强 + 移动端

| 周次 | 任务 | 验收 |
|---|---|---|
| W19 | AI 辅助生成文档/表单 | LLM 集成 |
| W20 | BI/数据透视表 | 数据分析 |
| W21 | IM 增强(频道管理/搜索) | IM 可用 |
| W22 | 移动端适配(响应式 Web) | 移动端可用 |
| W23 | 测试 + 演示 | 5 个 P0 故事全过 |

**新增用户故事**: US-618~622

---

## 六、总结

### 6.1 当前状态

- **P0 完成度**: 33/33 = 100% ✅
- **技术栈**: 三栈成熟，核心引擎完备
- **主要差距**: 文档/Wiki、知识库、集成市场、钉钉嵌入

### 6.2 下一步行动

1. **立即开始 Phase 1**: 文档/Wiki 核心（用户指定最高优先级）
2. **Phase 2 紧随**: 钉钉嵌入（用户指定钉钉优先）
3. **后续按序**: 集成市场 → 增强能力

### 6.3 关键决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 文档方案 | 复用 Collection 引擎 | 避免重复建设权限/ACL/工作流 |
| 嵌入平台 | 钉钉优先 | 用户指定 |
| 搜索方案 | PostgreSQL FTS | 避免引入 Elasticsearch 复杂度 |
| 编辑器 | Markdown | 与 Notion 对齐，社区成熟 |
| 移动端 | 响应式 Web 先 | 快速覆盖，后续再考虑原生 App |
| 产品命名 | DBCool | 避开 WorkPro 商标冲突 |
| NocoBase 许可 | 独立代码，Apache-2.0 | 自部署内部使用，完全合规 |

### 6.4 时间估算修正

| 阶段 | 原估算 | 修正后 | 理由 |
|---|---|---|---|
| Phase 1 (Wiki 核心) | 8 周 | **10 周** | Markdown 编辑器 + 版本管理 + 权限 + 搜索 + 工作流集成 |
| Phase 2 (钉钉嵌入) | 6 周 | **6 周** | SSO + 通讯录 + 审批 + 群消息，钉钉审核需预留时间 |
| Phase 3 (集成市场) | 4 周 | **4 周** | 市场 UI + 首批集成 |
| Phase 4 (增强能力) | 5 周 | **5 周** | AI + BI + IM 增强 + 移动端 |
| **总计** | **23 周** | **25 周** | 增加 2 周缓冲 |

---

## 七、参考文档

- `USER_STORIES.md` — 49 个用户故事 + 完成度审计
- `MVP_SCOPE.md` — MVP 范围定义
- `IDEA_BRIEF.md` — 产品定位与差异化
- `ROADMAP.md` — 阶段路线图
- `ARCHITECTURE.md` — 架构设计
- `CHANGELOG.md` — 开发日志
